Flask>=3.0.0
Werkzeug>=3.0.0
APScheduler>=3.10.0
requests>=2.31.0
python-dateutil>=2.8.0
pytz>=2023.3
Jinja2>=3.1.0
MarkupSafe>=2.1.0
itsdangerous>=2.1.0
click>=8.1.0
blinker>=1.6.0
qrcode[pil]>=7.4.0
Pillow>=10.0.0
psutil>=5.9.0
gunicorn>=21.0.0

# Dependências para melhorias (opcionais mas recomendadas)
# Se não tiver psutil, funciona sem monitoramento de sistema
# psutil>=5.9.0

# Para logs estruturados (opcional)
# structlog>=23.1.0

# Para cache distribuído futuro (opcional)
# redis>=4.5.0

# Para validação avançada futuro (opcional)
# marshmallow>=3.19.0

# Para sanitização de dados futuro (opcional)
# bleach>=6.0.0